package Subsystem;

public class Encoder {

    //Declaration of class variables
    //data_type Varriable name = inital_value;
    static final int MOTOR_TICK_COUNT = 1120;
    int motorTargetPosition;
    int distanceToBeMoved;

    //Constructor
    //function with name same as the class name
    public void Encoder(){
        int encoderDrivingTarget = (int)(rotationsNeeded * MOTOR_TICK_COUNT)
    }//end of constructor

    //Declare functions
    //scope return_type function_name(input arguments){}

    //function to reset motor

    //function to calculate target position

    //funciton to move motor to targetposition

}//end of class
