package Subsystem;

import com.qualcomm.robotcore.hardware.DcMotor;

public class MotorTickCount {

        public DcMotor sorterMotor1;
        public DcMotor sorterMotor2;

    }
    public MotorTickCount = 1120;(){
        leftMotor.setMode(DcMotor.RunMode.STOP_AND_RESET_ENCODER);
        distance ‘d’ / circumference; = πD. = 3.14 *diameter
        double rotationsNeeded = distance ‘d’ / circumference;
        Int encoderDrivingTarget = (int)(rotationsNeeded * MOTOR_TICK_COUNTS)
    }
    public targetPosition